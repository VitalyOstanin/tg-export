---
name: Отчёт об ошибке
about: Что-то работает не так, как описано
title: ''
labels: bug
---

<!--
ВНИМАНИЕ: не вставляйте сюда реальные данные Telegram.

Имена и идентификаторы чатов, номера телефонов, имена контактов, содержимое
сообщений и путь к файлу сессии — это персональные данные, и issue публичен.
Замените их вымышленными: «Chat_123», «Иван Иванов», «+70000000000».
Из вывода команд уберите строки с реальными именами; структура сообщения об
ошибке важнее его содержимого.

Не прикладывайте api_id, api_hash, содержимое api_credentials.yaml и сам файл
сессии: ключ авторизации из него даёт вход в аккаунт без второго фактора.

Если проблема касается безопасности, не заводите issue вовсе — см. SECURITY.md.
-->

## Что произошло

## Что ожидалось

## Как воспроизвести

1.
2.

## Окружение

- версия tg-export (`tg-export --version`):
- версия Python (`python --version`):
- операционная система:
- установка: PyPI / из исходников

## Вывод команды

<!-- Запустите команду в виде `tg-export --debug <команда> ...` и вставьте вывод,
убрав из него личные данные. Флаг `--debug` ставится ПЕРЕД подкомандой: он объявлен
у корневой группы, и `tg-export run --debug` завершится «No such option '--debug'». -->

```
```
