# Contributing

Спасибо за интерес к проекту! Этот документ описывает, как настроить окружение и какие конвенции приняты в репозитории.

## Содержание

- [Устройство пакета](#устройство-пакета)
- [Окружение](#окружение)
- [Запуск тестов](#запуск-тестов)
- [Проверки перед PR](#проверки-перед-pr)
- [Конвенции, которые удерживают тесты](#конвенции-которые-удерживают-тесты)
- [Схема базы состояния](#схема-базы-состояния)
- [Язык артефактов](#язык-артефактов)
- [Формат коммитов](#формат-коммитов)
- [Pull request](#pull-request)
- [Безопасность данных](#безопасность-данных)

## Устройство пакета

Поток одного экспорта: `cli` → `config` → `api` → `exporter` → `state` / `media` → `renderer`.
Команда разбирает аргументы и читает конфиг, поднимает соединение и базу состояния,
собирает `Exporter`, тот обходит чаты, складывает сообщения в SQLite и файлы на диск,
после чего рендерит HTML.

| №  | Модуль                    | Назначение                                                              |
|----|---------------------------|-------------------------------------------------------------------------|
| 1  | `cli/`                    | пакет команд: модуль на группу, `__init__.py` собирает их в `main`        |
| 2  | `config.py`               | чтение YAML-конфига и разрешение правил для конкретного чата              |
| 3  | `auth.py`                 | аккаунты: учётные данные, пути к файлам сессии и конфига                  |
| 4  | `api.py`                  | обёртка Telethon, режим Takeout, блокировка файла сессии                  |
| 5  | `session.py`              | подкласс `SQLiteSession`, обходящий порядок колонок в файле сессии        |
| 6  | `catalog.py`              | список чатов и генерация шаблона конфига по нему                          |
| 7  | `exporter.py`             | цикл экспорта: отбор чатов, две фазы выборки, живой статус                |
| 8  | `converter.py`            | перевод объектов Telethon в модели пакета                                 |
| 9  | `models.py`               | модели сообщения, медиа, служебных действий                               |
| 10 | `state.py`                | SQLite-состояние: сообщения, файлы, прогресс по чатам                     |
| 11 | `media.py`                | скачивание медиа, отбор по типу и размеру, повторное использование файлов |
| 12 | `importer.py`             | поиск уже скачанных файлов в экспорте Telegram Desktop                    |
| 13 | `html/renderer.py`        | рендер страниц через Jinja2                                               |
| 14 | `locking.py`              | advisory-блокировки: один процесс на файл сессии и на базу состояния      |
| 15 | `privacy.py`              | права доступа к файлам с секретами и личными данными                      |
| 16 | `format.py`               | форматирование размеров и скоростей                                       |
| 17 | `errors.py`               | доменные ошибки и коды возврата, которым они соответствуют                |
| 18 | `console.py`              | единственный `Console` вывода в stderr                                    |
| 19 | `verify.py`               | сверка файлов на диске с базой и перекачивание битых                      |

Границы импортов: стандартная библиотека импортируется в шапке модуля — она уже загружена
интерпретатором, и выигрыша от отложенного импорта нет. Внутри тел функций остаются только
тяжёлые зависимости и модули самого пакета (`telethon`, `jinja2`, `tg_export.exporter`,
`tg_export.state`): их загрузка на каждый `--help` стоит около секунды. Правило закреплено
тестом `test_standard_library_is_imported_at_module_level`.

Границы слоёв: схему базы знает только `state.py` — команды обращаются к ней методами,
а не собственным SQL. Разметку страниц задают шаблоны `html/templates/`, а не строки в
Python. Сетевые вызовы идут через `api.py`, чтобы режим Takeout и блокировка сессии
действовали для всех команд одинаково.

Граница между циклом событий и потоками проведена по стоимости операции, а не по её виду.
В поток (`asyncio.to_thread`) уходит всё, что читает или пишет содержимое: запросы к базам
соседних аккаунтов, копирование файлов из экспорта Telegram Desktop, `os.link`, рендер страниц,
обход каталога при уборке. Проверяется это тестами `tests/test_blocking_io.py`, которые смотрят,
в каком потоке операция фактически выполнилась. Операции с метаданными файловой системы вокруг
одного скачивания -- `mkdir`, `stat`, `exists`, `os.replace`, снос остатков staging -- остаются
на цикле намеренно: внутри них нет `await`, и только поэтому их последовательности
«проверил -- сделал» атомарны относительно других корутин, которые пишут в тот же каталог.
Перенос их в поток потребовал бы явной блокировки по каталогу назначения. Границу придётся
пересмотреть, если каталог выгрузки на сетевой файловой системе станет поддерживаемым
сценарием: там `mkdir` и `stat` стоят десятки миллисекунд, и на это время встают все
параллельные загрузки.

Отказы команд делятся по причине, и от причины зависит код возврата. Неверная форма
вызова -- взаимоисключающие опции, отсутствие обязательного аргумента, пустой набор того,
что команда должна получить, -- это `click.UsageError` (или `click.BadParameter` для одной
опции): click печатает блок usage и завершает процесс кодом 2, то есть пользователь сразу
видит, как команду следовало вызвать. Отказ по состоянию системы -- нет конфигурации, чат
не найден, файл занят -- это `_fail(...)` из `cli/common.py`: сообщение без блока usage и
код 1, потому что вызов был правильным. Правило закреплено тестом
`test_a_refusal_over_the_call_shape_gives_code_two_with_usage` (`tests/test_exit_codes.py`).

Design-документы, с которых начинался проект (`docs/superpowers/`), в репозиторий не входят
и остаются локальными записями замысла: они описывают состояние на момент проектирования и
с кодом не сверяются. Актуальная карта пакета — таблица выше, принятые решения — `docs/adr/`.

## Окружение

Минимальная версия — Python 3.11 (`requires-python = ">=3.11"`). CI прогоняет тесты на
3.11, 3.12, 3.13 и 3.14; линтер, проверка типов и границы покрытия считаются на каждой
версии матрицы, а на 3.12 дополнительно выгружается отчёт покрытия в Codecov.
Платформа проверки — Linux (`ubuntu-latest`). На macOS работа ожидается, но не
проверяется; на Windows не проверяется тем более: там нет `fcntl`, и блокировка файла
сессии вырождается в отсутствующую, а обработчики сигналов не регистрируются
(`loop.add_signal_handler` поднимает `NotImplementedError`, экспорт продолжается без них).

Проверка типов исполняется Node: пакет `pyright` несёт JavaScript, но не среду
выполнения. Берётся системный `node` из PATH, а при его отсутствии `nodeenv`
скачивает Node с `nodejs.org` при первом запуске -- то есть первый
`scripts/check.sh` на машине без Node требует сети помимо PyPI, а под запретом
исходящих соединений отказывает сообщением, к типам отношения не имеющим.
Остальные проверки самодостаточны.

Используется `uv` для управления зависимостями и виртуальным окружением.

```bash
# Установка uv: https://github.com/astral-sh/uv
uv sync
```

Инструменты разработки объявлены группой `dev` (PEP 735) и входят в набор `uv sync`
по умолчанию. Extra `proxy` -- напротив, необязательная зависимость самого пакета:
она нужна только при работе через прокси и ставится явно, `uv sync --extra proxy`.

Все команды разработки запускаются через `uv run`, без активации venv напрямую:

```bash
uv run tg-export --help
uv run python -m pytest
```

## Запуск тестов

```bash
uv run python -m pytest
```

Для подробного вывода:

```bash
uv run python -m pytest -v
```

Частичный прогон -- один файл или отбор по `-k` -- запускается с `--no-cov`:

```bash
uv run python -m pytest tests/test_models.py --no-cov
```

Причина: `--cov` включён в `addopts`, а порог покрытия применяется к результату
запуска целиком. Для одного файла покрытие заведомо ниже порога, и прогон
завершается кодом 1 при всех зелёных тестах; порог осмыслен только для полного набора.

Тесты не обращаются к Telegram API и не требуют сети.

Покрытие считается на каждом запуске (`--cov` задан в `addopts`), поэтому общий
порог из `[tool.coverage.report] fail_under` срабатывает и локально, а не только
в CI. Нижние границы по модулям проверяются отдельно -- их общий порог не ловит,
потому что хорошо покрытые мелкие файлы вытягивают среднее за крупные:

```bash
uv run python scripts/coverage_gate.py
```

Границы объявлены в `pyproject.toml`, секция `[tool.tg-export.coverage-floor]`,
и стоят чуть ниже фактических значений: они ловят падение покрытия, а не
требуют его роста. При осознанном снижении границу правят вместе с кодом.

Отставшая граница ловит только обвал, поэтому гейт отказывает и в обратную
сторону: если фактическое покрытие ушло больше чем на пять пунктов вверх, он
называет модуль и число, которое следует записать. Держать границы в этом
коридоре -- часть правки, поднявшей покрытие.

Новый модуль пакета требует своей строки в этой таблице -- граница ставится
на пять пунктов ниже фактического покрытия. Без неё `scripts/check.sh`
отказывает на последнем шаге, уже после линтера, типов и тестов.

## Проверки перед PR

Весь набор проверок, который выполняет CI, прогоняется одной командой:

```bash
scripts/check.sh
```

Порядок шагов тот же, что в `.github/workflows/ci.yml`: `uv lock --check`, `ruff check`,
`ruff format --check`, `pyright`, тесты с покрытием, границы покрытия по модулям.
Скрипт останавливается на первой упавшей проверке и возвращает её код.

Исправимое исправляется тем же скриптом -- он сначала прогоняет `ruff check --fix` и
`ruff format`, затем обычные проверки:

```bash
scripts/check.sh --fix
```

Расхождение скрипта с CI ловится тестом
`tests/test_code_style.py::test_one_command_runs_everything_ci_runs`: шаг, добавленный в
workflow и не добавленный в скрипт, роняет проверки.

Отдельные шаги, когда нужен только один из них:

```bash
uv run ruff check .
uv run ruff format --check .
uv run pyright
```

## Конвенции, которые удерживают тесты

Часть правил проекта проверяется автоматически: нарушение видно как красный тест, а не как
замечание при чтении. Обоснование каждого правила -- в docstring соответствующего теста
(`tests/test_code_style.py`); таблица нужна, чтобы правило было известно до того, как код
написан. Полнота таблицы проверяется тестом `test_every_style_rule_is_named_in_contributing`.

| №  | Правило                                                                 | Тест                                                             |
|----|-------------------------------------------------------------------------|------------------------------------------------------------------|
| 1  | вывод команд идёт через `click.echo`, а не `print`                      | `test_auth_uses_click_echo_not_bare_print`                       |
| 2  | `exporter._log` печатает через `console`, иначе вывод ломает Live       | `test_log_function_uses_rich_console_not_bare_print`             |
| 3  | из двух способов печати остаётся `console.print`                        | `test_no_console_log_calls_in_exporter`                          |
| 4  | `Live` ведётся `with`-блоком, а не ручными `__enter__`/`__exit__`       | `test_no_manual_live_enter_exit_in_exporter`                     |
| 5  | клиент Telegram создаётся только поверх `FixedSQLiteSession`            | `test_every_telegram_client_is_built_on_the_fixed_session`       |
| 6  | сообщение, за которым команда отказывает, переживает `--quiet`          | `test_messages_before_a_failure_exit_survive_quiet`              |
| 7  | Takeout-контекст ведётся `AsyncExitStack`, а не ручными вызовами        | `test_no_manual_async_context_calls_in_api`                      |
| 8  | разметка снимается средствами rich, без своего помощника                | `test_strip_markup_function_removed`                             |
| 9  | логгер модуля назван `logger` и объявлен после блока импортов           | `test_logger_declared_after_all_module_imports`                  |
| 10 | `timedelta` импортируется на уровне модуля                              | `test_timedelta_imported_at_module_level_in_exporter`            |
| 11 | в теле функции откладываются только тяжёлые зависимости                 | `test_standard_library_is_imported_at_module_level`              |
| 12 | пользовательский текст экранируется `rich.markup.escape`                | `test_exporter_imports_rich_escape`                              |
| 13 | инструменты разработки объявлены группой, а не extra                    | `test_development_tools_are_declared_as_a_group_not_as_an_extra` |
| 14 | каждый не-`.py` файл пакета попадает в дистрибутив                      | `test_every_package_data_file_is_declared_for_packaging`         |
| 15 | соединение и база состояния открываются только `async with`             | `test_cli_never_manages_connection_lifetime_by_hand`             |
| 16 | помощники подключения не отдают ресурс наружу                           | `test_cli_helpers_are_context_managers`                          |
| 17 | функция пакета не длиннее ста строк                                     | `test_no_function_is_longer_than_a_screenful`                    |
| 18 | SQL знает только слой состояния                                         | `test_cli_does_not_run_sql_of_its_own`                           |
| 19 | у каждого модуля `cli` есть строка назначения                           | `test_cli_has_a_module_docstring`                                |
| 20 | форматирование берётся из `format.py`, без псевдонимов                  | `test_cli_takes_formatting_helpers_from_their_own_module`        |
| 21 | текст читается в UTF-8, а не в кодировке локали                         | `test_every_text_file_is_read_as_utf8`                           |
| 22 | новая команда описана в `docs/cli.md`                                   | `test_every_command_is_described_in_the_cli_reference`           |
| 23 | `scripts/check.sh` покрывает все проверки CI                            | `test_one_command_runs_everything_ci_runs`                       |
| 24 | команда завершается через `ctx.exit()`, не через внутренний класс click | `test_cli_does_not_use_click_private_exit_exception`             |
| 25 | имя класса не дублируется строковым литералом рядом с ним               | `test_no_action_repeats_its_class_name_as_a_string`              |
| 26 | в `models.py` не остаётся типов, которых никто не строит                | `test_models_do_not_keep_types_nobody_builds`                    |
| 27 | имя не импортируется дважды -- в шапке и в теле функции                 | `test_no_module_imports_the_same_name_twice`                     |
| 28 | код пакета и файлы сборки написаны по-английски                         | `test_the_package_and_its_build_files_speak_english`             |
| 29 | команда, названная в строках кода, существует в интерфейсе              | `test_every_command_named_in_the_code_exists`                    |
| 30 | тело функции не разрывается пустой строкой ни до docstring, ни после     | `test_no_function_body_starts_with_a_blank_line`                 |
| 31 | файл не заканчивается баннером-разделителем                             | `test_no_module_ends_with_a_divider_banner`                      |
| 32 | статус файла записывается членом `FileStatus`, не строкой               | `test_file_statuses_are_spelled_by_the_enum_not_as_free_strings` |
| 33 | `FileStatus` и `DownloadStatus` совпадают там, где пересекаются         | `test_the_two_status_vocabularies_agree_where_they_overlap`      |
| 34 | константа объявлена выше функции, которая её читает                     | `test_no_module_constant_is_read_above_its_declaration`          |
| 35 | обход сообщений чата объявлен один раз на обе фазы экспорта              | `test_the_message_walk_is_written_once`                          |
| 36 | docstring и сообщения проверок в `tests/` написаны по-русски            | `test_the_tests_speak_russian`                                   |
| 37 | вопросы пользователю задаются помощниками `ask`/`confirm`, не в stdout   | `test_no_question_is_asked_on_stdout`                            |
| 38 | объявленная константа модуля где-то читается                            | `test_no_module_constant_is_declared_and_never_read`             |
| 39 | типы, записываемые через `vars()`, не содержат полей-dataclass          | `test_types_written_with_vars_stay_flat`                         |
| 40 | формат даты и времени берётся из `format.py`, а не пишется литералом    | `test_no_time_format_is_spelled_as_a_literal`                    |
| 41 | группы команд не зовут имён с подчёркиванием из соседних модулей        | `test_no_command_group_reaches_for_an_underscored_name_of_another_module` |
| 42 | проверки стиля берут файлы из общих множеств, а не своим обходом        | `test_style_rules_take_their_files_from_the_shared_sets`         |
| 43 | проверка внутри многострочного шага CI видна сверке с `scripts/check.sh` | `test_a_check_inside_a_multi_line_step_is_seen_by_the_parity_check` |
| 44 | каждая опция команды описана в её разделе `docs/cli.md`                | `test_every_option_of_every_command_is_named_in_the_cli_reference` |

## Схема базы состояния

Схема объявлена одной строкой-константой `SCHEMA_SQL` в `tg_export/state.py`. Колонка,
добавленная в это объявление, применяется и к базам, созданным прежними версиями:
при открытии фактический состав таблиц сверяется с объявленным, недостающие колонки
добавляются `ALTER TABLE`. Поэтому новая колонка обязана иметь значение по умолчанию
или допускать `NULL` -- иначе добавить её к существующей таблице нельзя.

Изменение, которое добавлением колонки не решается (удаление колонки, смена типа,
перестроенный индекс), требует отдельного шага миграции и увеличения `SCHEMA_VERSION`:
версия хранится в файле как `PRAGMA user_version`.

Частью формата хранения являются и имена в коде. Колонки `media`, `action`, `text_parts`,
`forwarded_from`, `reactions` и `inline_buttons` таблицы `messages` объявлены как `TEXT` и
хранят JSON, собранный по dataclass: имя класса лежит в служебном ключе (`__media_class__`,
`__action_class__`), имена полей -- это остальные ключи, поскольку чтение выполняется как
`cls(**d)`; дата записывается как `{"__datetime__": <isoformat>}`, значение перечисления --
своим значением. Поэтому переименование dataclass или его поля в `tg_export/models.py` делает
нечитаемыми записи прежних выгрузок ровно так же, как правка DDL, и требует того же внимания:
ни проверка типов, ни тесты этого не покажут, потому что колонки объявлены текстом.

## Язык артефактов

Язык выбирается по читателю артефакта, а не по файлу.

| № | Артефакт                                                            | Язык       |
|---|---------------------------------------------------------------------|------------|
| 1 | Код пакета `tg_export/`: комментарии, docstrings, имена              | английский |
| 2 | Вывод команд и тексты ошибок                                         | английский |
| 3 | Манифест `pyproject.toml`, скрипты `scripts/`, workflow `.github/`   | английский |
| 4 | Тесты `tests/`: docstrings и сообщения проверок                      | русский    |
| 5 | Документация: README, `docs/`, CHANGELOG, ADR, этот файл             | русский    |
| 6 | Шаблоны issue и pull request                                         | русский    |
| 7 | Сообщения коммитов                                                   | русский    |

Смысл разделения: строки 1--3 читает всякий, кто берёт пакет с PyPI или открывает
репозиторий со стороны, поэтому там английский; строки 4--7 обращены к тому, кто ведёт
проект. Строки 1--3 закреплены тестом
`tests/test_code_style.py::test_the_package_and_its_build_files_speak_english`: кириллица в
коде пакета, манифесте, скриптах или workflow роняет проверки.

## Формат коммитов

Используется [Conventional Commits](https://www.conventionalcommits.org/ru/v1.0.0/):

```
<тип>(<область>): краткое описание

Опциональное расширенное описание.
```

Типы: `feat`, `fix`, `chore`, `refactor`, `docs`, `test`, `perf`, `ci`.

Примеры из истории:

- `feat(tg): показывать msg_id в выводе messages, дедупликация файлов в download`
- `fix(terminal): эскейп rich-markup в именах чатов/файлов и устранение гонки в Live`
- `chore: подготовка к релизу v1.1.0`

## Pull request

- Один PR -- одна логическая задача. Если нужно несколько правок, разбейте на отдельные PR.
- Все тесты должны проходить перед merge.
- В описании PR кратко: что изменено и зачем, какие тесты добавлены. Шаблон с перечнем проверок
  подставляется автоматически (`.github/PULL_REQUEST_TEMPLATE.md`).
- Не добавляйте в репозиторий реальные данные Telegram (имена контактов, чатов, ID, экспортированные файлы).
- Артефакты сборки (`build/`, `dist/`, `*.egg-info`, `.coverage`) в git не попадают, но остаются в рабочем
  дереве и сбивают поиск по репозиторию: `build/lib/tg_export/` — копия пакета на момент сборки, и `rg` по
  имени функции находит её дважды. Убирать их командой `rm -rf build/ dist/ *.egg-info`.

## Безопасность данных

В коде, тестах, фикстурах, коммитах и любых файлах проекта запрещено использовать:

- реальные имена пользователей, контактов, чатов, групп, каналов;
- реальные Telegram ID;
- реальные api_id/api_hash, session-файлы;
- результаты экспорта `export_output/` (этот каталог и любые другие, содержащие реальные данные).

Используйте только вымышленные данные в примерах и тестах.

Найденную уязвимость не заводите публичным issue: порядок приватного сообщения
описан в [SECURITY.md](SECURITY.md).
